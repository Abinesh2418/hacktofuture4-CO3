fastapi>=0.115.0
uvicorn[standard]>=0.30.6
pydantic>=2.9.2
pydantic-settings>=2.5.2
python-dotenv>=1.0.1
httpx>=0.27.2
websockets==13.1
loguru==0.7.2

# CrewAI multi-agent framework
# CrewAI 1.x split litellm out of the core install — request the extra so
# the `nvidia_nim/`, `azure/`, etc. provider routing is wired up.
crewai[litellm]>=1.14.0
litellm>=1.51.0
azure-ai-inference>=1.0.0b9
openai>=1.0.0
xmltodict>=0.13.0

# MCP client
fastmcp>=0.4.0
loguru>=0.7.2

# Tests
pytest>=7.0.0
pytest-asyncio>=0.23.0
